pipeline {
    agent any
 
    stages {
       stage('Build') {
            steps {
                echo "multiple repository example"
            }
        }
         
        // Add more stages as needed
    }
}
